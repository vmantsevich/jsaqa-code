{
  "viewportWidth": 360,
  "viewportHeight": 640,
  "baseUrl": "http://localhost:3000",
  "retries": 2,
  "scripts": {
    "cy:run:chrome1": "cypress run --browser chrome --headless",
    "cy:run:electron1": "cypress run --browser electron --headless",
    "cy:run:chromeMobile": "cypress run --browser chrome --config-file cypress.env.json --headed",
    "cy:run:electronMobile": "cypress run --browser electron --headed --config-file cypress.env.json"
  }
}
